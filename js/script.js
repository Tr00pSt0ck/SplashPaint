const canvas=document.getElementById('bg-canvas');const ctx=canvas.getContext('2d');let W,H,particles=[];const COLORS=['#ff3cac','#784bff','#2bd9fe'];function resize(){W=canvas.width=window.innerWidth;H=canvas.height=window.innerHeight;}
resize();window.addEventListener('resize',resize);class Particle{constructor(){this.reset(true);}
reset(init){this.x=Math.random()*W;this.y=init?Math.random()*H:H+10;this.r=Math.random()*1.5+0.3;this.speed=Math.random()*0.4+0.1;this.color=COLORS[Math.floor(Math.random()*COLORS.length)];this.opacity=Math.random()*0.5+0.1;this.drift=(Math.random()-0.5)*0.3;}
update(){this.y-=this.speed;this.x+=this.drift;if(this.y<-10)this.reset(false);}
draw(){ctx.beginPath();ctx.arc(this.x,this.y,this.r,0,Math.PI*2);ctx.fillStyle=this.color;ctx.globalAlpha=this.opacity;ctx.fill();}}
for(let i=0;i<80;i++)particles.push(new Particle());function animCanvas(){ctx.clearRect(0,0,W,H);particles.forEach(p=>{p.update();p.draw();});ctx.globalAlpha=1;requestAnimationFrame(animCanvas);}
animCanvas();window.addEventListener('load',()=>{const lbar=document.getElementById('lbar');const loader=document.getElementById('loader');const loaderLogo=document.querySelector('.loader-logo');const loaderSub=document.querySelector('.loader-sub');gsap.to(loaderLogo,{opacity:1,y:0,duration:0.6,ease:'expo.out',delay:0.1});gsap.to(loaderSub,{opacity:1,duration:0.4,delay:0.3});gsap.to(lbar,{width:'100%',duration:1.2,ease:'power2.inOut',delay:0.2,onComplete:()=>{gsap.to(loader,{opacity:0,duration:0.6,delay:0.15,ease:'power2.in',onComplete:()=>{loader.style.display='none';revealPage();}});}});});function revealPage(){const page=document.getElementById('page');const nav=document.getElementById('nav');gsap.to(page,{opacity:1,duration:0.5});gsap.to(nav,{y:0,duration:0.7,ease:'expo.out',delay:0.1});gsap.to('#hero-eyebrow',{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:0.3});gsap.to('#hero-title',{opacity:1,y:0,duration:0.9,ease:'expo.out',delay:0.5});gsap.to('#hero-sub',{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:0.8});gsap.to('#hero-actions',{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:1.0});gsap.to('#scroll-hint',{opacity:1,duration:0.6,delay:1.5});gsap.from('#stats-inner',{y:50,opacity:0,duration:0.8,ease:'expo.out',delay:1.1});gsap.registerPlugin(ScrollTrigger);document.querySelectorAll('.reveal').forEach(el=>{gsap.to(el,{opacity:1,y:0,duration:0.8,ease:'expo.out',scrollTrigger:{trigger:el,start:'top 88%',once:true}});});document.querySelectorAll('.feature-card').forEach((card,i)=>{gsap.to(card,{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:i*0.08,scrollTrigger:{trigger:card,start:'top 90%',once:true}});});document.querySelectorAll('.step').forEach((step,i)=>{gsap.to(step,{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:i*0.15,scrollTrigger:{trigger:'.steps-row',start:'top 85%',once:true}});});document.querySelectorAll('.t-card').forEach((card,i)=>{gsap.to(card,{opacity:1,y:0,duration:0.7,ease:'expo.out',delay:i*0.1,scrollTrigger:{trigger:card,start:'top 90%',once:true}});});}
document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(!target)return;e.preventDefault();const offset=target.getBoundingClientRect().top+window.scrollY-80;window.scrollTo({top:offset,behavior:'smooth'});});});window.addEventListener('scroll',()=>{const nav=document.getElementById('nav');if(window.scrollY>40){nav.style.background='rgba(10,10,15,0.92)';}else{nav.style.background='rgba(10,10,15,0.75)';}});